---
name: pi-repo-optimize
description: 对配置仓库（如 .pi）做目录结构、文件存储、架构层面的摸底分析与分级优化执行。用户说"检查 .pi 目录""结构优化""仓库卫生""存储清理"时触发。不适用：代码正确性/回归审计用 pi-full-audit。
version: v1.1
更新日期: 2026-09-12
---

# 配置仓库深度优化（.pi 类）

对配置仓库（目录/存储/架构层面）做完整优化：摸底 → 分级方案 → 用户确认删除类条目 → 执行落地 → 验证提交。已验证实例：2026-08-25 .pi 仓库优化（分级方案 + drafts 入库 + 仓库卫生提交 e0c708e）。

## 元信息

| 属性 | 值 |
|------|-----|
| 版本 | v1.1 |
| 更新日期 | 2026-09-12 |
| 适用场景 | 用户要求对配置仓库做结构/存储/架构层面的检查优化 |
| 不适用 | 代码正确性/回归审计（用 pi-full-audit）；单点问题排查（直接 grep/read） |
| 依赖 | 无 |

---

## 目录

- [一、概述](#一概述)
- [二、工作流](#二工作流)
- [三、详细步骤](#三详细步骤)
- [四、关键要点](#四关键要点)
- [五、使用示例](#五使用示例)
- [六、常见问题](#六常见问题)
- [七、更新记录](#七更新记录)

---

## 一、概述

### 1.1 解决的问题

配置仓库（如 .pi）在长期使用过程中会出现：
- 目录结构混乱
- 存储空间浪费
- 架构设计不合理

### 1.2 设计理念

- **摸底先行**：分析阶段只读，方案先行
- **分级优化**：按 H1/H2/H3 分级处理
- **用户确认**：删除类条目需用户确认
- **一次一提交**：避免碎提交

---

## 二、工作流

```
摸底（只读） → 输出分级方案 → 用户确认删除类条目 → 执行与验证 → 收尾汇报
```

---

## 三、详细步骤

### 第 1 步：摸底（只读）

- 目录树全览（含隐藏目录），标注每块职责：配置/扩展/运行时/文档/部署
- git status + .gitignore 核对：运行时数据（sessions/logs/checkpoints/stats）是否已忽略；lock 文件是否入库；ignore 规则是否重复
- 大文件与残留目录扫描（bak/、旧工具目录、迁移遗留）
- 明确区分两类文件：入库共享（配置、源码、README）vs 运行时本地（会话、日志、统计）

### 第 2 步：输出分级方案

- **H1 必改**：结构错误、gitignore 冲突、存在丢数据/覆盖风险
- **H2 建议**：仓库卫生（忽略规则、去重、残留清理）、文档同步（README 与代码实际对齐）
- **H3 可选**：重构迁移、长期演进
- 每条附：位置、问题、改法、影响/风险

### 第 3 步：删除类条目用户确认

- 涉及删除运行时数据或跨设备文件的条目单列，执行前逐项确认"移除/保留"
- 实例：清理方案提议移除远程主机统计文件（memory/stats/tool-use-MYPC.jsonl），用户明确保留——远程设备的统计文件即使 git 忽略也不删

### 第 4 步：执行与验证

- 按用户确认的方案批量修改，一轮内完成同类改动
- 验证：配置仓库跑 scripts/test/test-all.sh 全量回归
- 文档同步：README/AGENTS.md 的目录清单与实际一致
- 提交推送前检查 remote 无 token（有则先恢复无凭证 URL）

### 第 5 步：收尾汇报

- 按 H1/H2/H3 分级汇报落地情况，标注未执行项及原因
- 未确认的删除类条目保持原样，不擅自处理

---

## 四、关键要点

- **分析阶段只读，方案先行**；执行前把"要删什么"讲清楚，让用户做决策，而不是直接删
- **运行时数据与入库数据的边界是优化重点**：该入库的（lock、有效配置）补入，不该入库的（会话、统计）忽略
- **一次优化会话产出 1 个提交粒度**（卫生类合并提交），避免碎提交

---

## 五、使用示例

### 5.1 触发场景

用户说：
- "检查 .pi 目录"
- "结构优化"
- "仓库卫生"
- "存储清理"

### 5.2 执行流程

```bash
# 1. 摸底
find ~/.pi -type f -name "*.log" | head -20
git status

# 2. 输出分级方案
# H1: 结构错误
# H2: 仓库卫生
# H3: 重构迁移

# 3. 用户确认
# 确认删除类条目

# 4. 执行与验证
bash scripts/test/test-all.sh

# 5. 收尾汇报
# 按 H1/H2/H3 分级汇报
```

---

## 六、常见问题

- **Q1**: 如何区分入库共享和运行时本地文件？
  **A**: 入库共享文件包括配置、源码、README；运行时本地文件包括会话、日志、统计。

- **Q2**: 删除类条目需要用户确认吗？
  **A**: 是的，涉及删除运行时数据或跨设备文件的条目需用户确认。

- **Q3**: 优化后如何验证？
  **A**: 运行 `bash scripts/test/test-all.sh` 全量回归。

---

## 七、更新记录

| 日期 | 版本 | 变更 |
|------|------|------|
| 2026-09-12 | v1.1 | 按照文档模板重新组织结构，添加元信息、目录导航、章节编号 |
| 2026-08-25 | v1.0 | 初始版本，实现配置仓库深度优化 |