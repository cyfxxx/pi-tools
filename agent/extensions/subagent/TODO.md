# TODO — subagent

## 下一个版本
- [ ] 支持 chain/parallel 的 async 模式（当前仅 single 支持）
- [ ] 链式变量 `{chain_dir}` 和 `{outputs.name}` 支持
- [ ] 进度追踪 progress.md 写入

## 长远
- [ ] Forked context (`context: "fork"`) 分支会话模式
- [ ] Console/JSON 模式下 async 完成通知（当前仅 TUI notify）
- [ ] 异步结果自动清理旧文件策略
